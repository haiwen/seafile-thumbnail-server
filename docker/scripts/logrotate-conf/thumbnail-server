/opt/seafile/logs/seafile.log
{
        daily
        missingok
        rotate 7
        # compress
        # delaycompress
        dateext
        dateformat .%Y-%m-%d
        notifempty
        create 644 root root
        sharedscripts
        postrotate
                if [ -f /opt/seafile/pids/seafile.pid ]; then
                        kill -USR1 `cat /opt/seafile/pids/seafile.pid`
                fi

        endscript
}
